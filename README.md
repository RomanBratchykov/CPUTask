# CPU Load Example
- ```bazel build //:cpu_load``` to build
- ```bazel run //:cpu_load``` to run

must create a file called cpu_load.txt in root directory for program to write to it,when program asks for it enter ```cpu_load.txt```